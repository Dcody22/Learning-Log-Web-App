"""
  Run this line to start project - note (must include period)
    django-admin startproject "project name" .
  
  
  Test server with this line - click link in terminal to see if server is running correctly on local server. 
    python3 manage.py runserver 
  
  Set up apps in the server 
    python3 manage.py startapp MainApp
  
  Migrate changes made in app into the database 
  first:
    python3 manage.py makemigrations MainApp
  second: 
    python3 manage.py migrate 
    
    
Installed Moduels:
 
pip install django 

URLS py is the adress and what view is assoicated with the language 
views to dislay app and link to databases 
html file to create the user view
"""   
